# Kopiere diese Datei nach .env und setze einen langen Zufallswert für SECRET_KEY
# Generieren z.B. mit: python3 -c "import secrets; print(secrets.token_hex(32))"

SECRET_KEY=change-me-to-a-long-random-string
